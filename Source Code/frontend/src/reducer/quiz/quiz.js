const quizReducer = (state = [], action) => {
    switch (action) {
        case 'FLAG_CURRENT':
            return state;
        default:
            return state;
    }
}

export default quizReducer;